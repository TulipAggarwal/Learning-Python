#Tuples in python

coordinates = (2,3,5) #creating a tuple
#Tuples are immutable which means they cannot be modified
print(coordinates[2]) #prints the element at 2nd index in our tuple

#We use tuples for storing data that is never going to change
